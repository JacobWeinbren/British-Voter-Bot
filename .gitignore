# Secrets
.env

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Raw BES data and documentation (2 GB; download from britishelectionstudy.com)
data/raw/
data/processed/

# Rendered cards are produced at post time; keep a few previews only
outputs/cards/
outputs/previews/*.html

# Cloudflare Worker (the hourly trigger): local build state, never the config or the source
trigger/node_modules/
trigger/.wrangler/
trigger/.dev.vars

# OS
.DS_Store
